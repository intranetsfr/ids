{% for size in sizes %}
<div class="ids-grid">
    <div class="ids-cell ids-cell--6-col">
        <span class="ids-fs--{{size}}-desktop">ids-fs--{{size}}-desktop</span>
    </div>
</div>
{% endfor %}